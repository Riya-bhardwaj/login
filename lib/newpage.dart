import 'package:flutter/material.dart';
class Newpage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("welcome ",style: TextStyle(fontSize: 30),),),);
  }
}
